diff --git a/old/README.md b/old/README.md
new file mode 100644
index 0000000..0fcee8b
--- /dev/null
+++ b/old/README.md
@@ -0,0 +1,11 @@
+# Legacy code
+
+This directory contains pre-rebuild SoC Proxy research code retained for
+reference only. It is intentionally outside the active `scripts/` and `src/`
+trees so that obsolete dispatch, clustering, optimisation, plotting, and
+analysis paths do not appear to be part of the current implementation.
+
+Code should only be moved back into the active tree when it is deliberately
+reused in the rebuilt paper workflow. Prefer implementing a fresh, narrowly
+scoped module and consulting these files for historical logic rather than
+reviving the old architecture wholesale.
diff --git a/scripts/experiments/post_cluster_optimisation/__init__.py b/old/scripts/experiments/post_cluster_optimisation/__init__.py
similarity index 100%
rename from scripts/experiments/post_cluster_optimisation/__init__.py
rename to old/scripts/experiments/post_cluster_optimisation/__init__.py
diff --git a/scripts/experiments/post_cluster_optimisation/optimisation.py b/old/scripts/experiments/post_cluster_optimisation/optimisation.py
similarity index 100%
rename from scripts/experiments/post_cluster_optimisation/optimisation.py
rename to old/scripts/experiments/post_cluster_optimisation/optimisation.py
diff --git a/scripts/experiments/post_cluster_optimisation/targets.py b/old/scripts/experiments/post_cluster_optimisation/targets.py
similarity index 100%
rename from scripts/experiments/post_cluster_optimisation/targets.py
rename to old/scripts/experiments/post_cluster_optimisation/targets.py
diff --git a/scripts/experiments/proxy_diagnostics/__init__.py b/old/scripts/experiments/proxy_diagnostics/__init__.py
similarity index 100%
rename from scripts/experiments/proxy_diagnostics/__init__.py
rename to old/scripts/experiments/proxy_diagnostics/__init__.py
diff --git a/scripts/experiments/proxy_diagnostics/margin_testing.py b/old/scripts/experiments/proxy_diagnostics/margin_testing.py
similarity index 100%
rename from scripts/experiments/proxy_diagnostics/margin_testing.py
rename to old/scripts/experiments/proxy_diagnostics/margin_testing.py
diff --git a/scripts/experiments/proxy_diagnostics/proxy_testing.py b/old/scripts/experiments/proxy_diagnostics/proxy_testing.py
similarity index 100%
rename from scripts/experiments/proxy_diagnostics/proxy_testing.py
rename to old/scripts/experiments/proxy_diagnostics/proxy_testing.py
diff --git a/scripts/experiments/representative_days/__init__.py b/old/scripts/experiments/representative_days/__init__.py
similarity index 100%
rename from scripts/experiments/representative_days/__init__.py
rename to old/scripts/experiments/representative_days/__init__.py
diff --git a/scripts/experiments/representative_days/comparison.py b/old/scripts/experiments/representative_days/comparison.py
similarity index 100%
rename from scripts/experiments/representative_days/comparison.py
rename to old/scripts/experiments/representative_days/comparison.py
diff --git a/scripts/experiments/seasonal_sampling.py b/old/scripts/experiments/seasonal_sampling.py
similarity index 100%
rename from scripts/experiments/seasonal_sampling.py
rename to old/scripts/experiments/seasonal_sampling.py
diff --git a/scripts/experiments/signal_analysis/__init__.py b/old/scripts/experiments/signal_analysis/__init__.py
similarity index 100%
rename from scripts/experiments/signal_analysis/__init__.py
rename to old/scripts/experiments/signal_analysis/__init__.py
diff --git a/scripts/experiments/signal_analysis/build_cache.py b/old/scripts/experiments/signal_analysis/build_cache.py
similarity index 100%
rename from scripts/experiments/signal_analysis/build_cache.py
rename to old/scripts/experiments/signal_analysis/build_cache.py
diff --git a/scripts/experiments/signal_analysis/graphs.py b/old/scripts/experiments/signal_analysis/graphs.py
similarity index 100%
rename from scripts/experiments/signal_analysis/graphs.py
rename to old/scripts/experiments/signal_analysis/graphs.py
diff --git a/scripts/experiments/signal_analysis/plot_results.py b/old/scripts/experiments/signal_analysis/plot_results.py
similarity index 100%
rename from scripts/experiments/signal_analysis/plot_results.py
rename to old/scripts/experiments/signal_analysis/plot_results.py
diff --git a/scripts/figures/__init__.py b/old/scripts/figures/__init__.py
similarity index 100%
rename from scripts/figures/__init__.py
rename to old/scripts/figures/__init__.py
diff --git a/scripts/figures/paper/__init__.py b/old/scripts/figures/paper/__init__.py
similarity index 100%
rename from scripts/figures/paper/__init__.py
rename to old/scripts/figures/paper/__init__.py
diff --git a/scripts/figures/paper/vertical_stacking.py b/old/scripts/figures/paper/vertical_stacking.py
similarity index 100%
rename from scripts/figures/paper/vertical_stacking.py
rename to old/scripts/figures/paper/vertical_stacking.py
diff --git a/scripts/helpers/compare_models.py b/old/scripts/helpers/compare_models.py
similarity index 100%
rename from scripts/helpers/compare_models.py
rename to old/scripts/helpers/compare_models.py
diff --git a/scripts/helpers/signals.py b/old/scripts/helpers/signals.py
similarity index 100%
rename from scripts/helpers/signals.py
rename to old/scripts/helpers/signals.py
diff --git a/scripts/helpers/visualisation/__init__.py b/old/scripts/helpers/visualisation/__init__.py
similarity index 100%
rename from scripts/helpers/visualisation/__init__.py
rename to old/scripts/helpers/visualisation/__init__.py
diff --git a/scripts/helpers/visualisation/adapter.py b/old/scripts/helpers/visualisation/adapter.py
similarity index 100%
rename from scripts/helpers/visualisation/adapter.py
rename to old/scripts/helpers/visualisation/adapter.py
diff --git a/scripts/helpers/visualisation/metrics.py b/old/scripts/helpers/visualisation/metrics.py
similarity index 100%
rename from scripts/helpers/visualisation/metrics.py
rename to old/scripts/helpers/visualisation/metrics.py
diff --git a/scripts/helpers/visualisation/plot_soc_comparison.py b/old/scripts/helpers/visualisation/plot_soc_comparison.py
similarity index 100%
rename from scripts/helpers/visualisation/plot_soc_comparison.py
rename to old/scripts/helpers/visualisation/plot_soc_comparison.py
diff --git a/scripts/helpers/visualisation/plot_soc_vs_proxy.py b/old/scripts/helpers/visualisation/plot_soc_vs_proxy.py
similarity index 100%
rename from scripts/helpers/visualisation/plot_soc_vs_proxy.py
rename to old/scripts/helpers/visualisation/plot_soc_vs_proxy.py
diff --git a/scripts/helpers/visualisation/plot_tsa_timeseries.py b/old/scripts/helpers/visualisation/plot_tsa_timeseries.py
similarity index 100%
rename from scripts/helpers/visualisation/plot_tsa_timeseries.py
rename to old/scripts/helpers/visualisation/plot_tsa_timeseries.py
diff --git a/scripts/helpers/visualisation/visualise.py b/old/scripts/helpers/visualisation/visualise.py
similarity index 100%
rename from scripts/helpers/visualisation/visualise.py
rename to old/scripts/helpers/visualisation/visualise.py
diff --git a/scripts/model_runs/__init__.py b/old/scripts/model_runs/__init__.py
similarity index 100%
rename from scripts/model_runs/__init__.py
rename to old/scripts/model_runs/__init__.py
diff --git a/scripts/model_runs/reference_dispatcher.py b/old/scripts/model_runs/reference_dispatcher.py
similarity index 100%
rename from scripts/model_runs/reference_dispatcher.py
rename to old/scripts/model_runs/reference_dispatcher.py
diff --git a/scripts/model_runs/tsa_dispatcher.py b/old/scripts/model_runs/tsa_dispatcher.py
similarity index 100%
rename from scripts/model_runs/tsa_dispatcher.py
rename to old/scripts/model_runs/tsa_dispatcher.py
diff --git a/scripts/preprocessing/__init__.py b/old/scripts/preprocessing/__init__.py
similarity index 100%
rename from scripts/preprocessing/__init__.py
rename to old/scripts/preprocessing/__init__.py
diff --git a/scripts/preprocessing/case_studies.py b/old/scripts/preprocessing/case_studies.py
similarity index 100%
rename from scripts/preprocessing/case_studies.py
rename to old/scripts/preprocessing/case_studies.py
diff --git a/scripts/preprocessing/clear_data.py b/old/scripts/preprocessing/clear_data.py
similarity index 100%
rename from scripts/preprocessing/clear_data.py
rename to old/scripts/preprocessing/clear_data.py
diff --git a/scripts/preprocessing/gb_demand.py b/old/scripts/preprocessing/gb_demand.py
similarity index 100%
rename from scripts/preprocessing/gb_demand.py
rename to old/scripts/preprocessing/gb_demand.py
diff --git a/scripts/preprocessing/shuffle_years.py b/old/scripts/preprocessing/shuffle_years.py
similarity index 100%
rename from scripts/preprocessing/shuffle_years.py
rename to old/scripts/preprocessing/shuffle_years.py
diff --git a/src/soc_proxy/calliope/__init__.py b/old/src/soc_proxy/calliope/__init__.py
similarity index 100%
rename from src/soc_proxy/calliope/__init__.py
rename to old/src/soc_proxy/calliope/__init__.py
diff --git a/src/soc_proxy/calliope/io.py b/old/src/soc_proxy/calliope/io.py
similarity index 100%
rename from src/soc_proxy/calliope/io.py
rename to old/src/soc_proxy/calliope/io.py
diff --git a/src/soc_proxy/calliope/model_config.py b/old/src/soc_proxy/calliope/model_config.py
similarity index 100%
rename from src/soc_proxy/calliope/model_config.py
rename to old/src/soc_proxy/calliope/model_config.py
diff --git a/src/soc_proxy/calliope/timeseries.py b/old/src/soc_proxy/calliope/timeseries.py
similarity index 100%
rename from src/soc_proxy/calliope/timeseries.py
rename to old/src/soc_proxy/calliope/timeseries.py
diff --git a/src/soc_proxy/clustering.py b/old/src/soc_proxy/clustering.py
similarity index 100%
rename from src/soc_proxy/clustering.py
rename to old/src/soc_proxy/clustering.py
diff --git a/src/soc_proxy/helpers/__init__.py b/old/src/soc_proxy/helpers/__init__.py
similarity index 100%
rename from src/soc_proxy/helpers/__init__.py
rename to old/src/soc_proxy/helpers/__init__.py
diff --git a/src/soc_proxy/helpers/normalise.py b/old/src/soc_proxy/helpers/normalise.py
similarity index 100%
rename from src/soc_proxy/helpers/normalise.py
rename to old/src/soc_proxy/helpers/normalise.py
diff --git a/src/soc_proxy/optimisation/__init__.py b/old/src/soc_proxy/optimisation/__init__.py
similarity index 100%
rename from src/soc_proxy/optimisation/__init__.py
rename to old/src/soc_proxy/optimisation/__init__.py
diff --git a/src/soc_proxy/optimisation/biases.py b/old/src/soc_proxy/optimisation/biases.py
similarity index 100%
rename from src/soc_proxy/optimisation/biases.py
rename to old/src/soc_proxy/optimisation/biases.py
diff --git a/src/soc_proxy/optimisation/dispatch.py b/old/src/soc_proxy/optimisation/dispatch.py
similarity index 100%
rename from src/soc_proxy/optimisation/dispatch.py
rename to old/src/soc_proxy/optimisation/dispatch.py
diff --git a/src/soc_proxy/optimisation/tsa.py b/old/src/soc_proxy/optimisation/tsa.py
similarity index 100%
rename from src/soc_proxy/optimisation/tsa.py
rename to old/src/soc_proxy/optimisation/tsa.py
diff --git a/src/soc_proxy/tsa_model.py b/old/src/soc_proxy/tsa_model.py
similarity index 100%
rename from src/soc_proxy/tsa_model.py
rename to old/src/soc_proxy/tsa_model.py
diff --git a/scripts/helpers/timeseries.py b/scripts/helpers/timeseries.py
new file mode 100644
index 0000000..abaa8e2
--- /dev/null
+++ b/scripts/helpers/timeseries.py
@@ -0,0 +1,238 @@
+"""Adapters for the fixed Calliope timeseries CSV format used in this project.
+
+Calliope input files contain several metadata rows before the hourly data. The
+rest of the SoC Proxy workflow should not need to understand that format, so
+this module converts between the Calliope CSV representation and a simple
+``DatetimeIndex`` dataframe.
+
+Model horizons use a half-open interval: ``[start, end)``.
+"""
+
+from __future__ import annotations
+
+import csv
+from pathlib import Path
+from typing import Iterable
+
+import pandas as pd
+
+
+EXPECTED_FEATURE_COLUMNS = (
+    "demand_power",
+    "solar",
+    "offshore_wind",
+    "onshore_wind",
+)
+
+
+def read_calliope_timeseries(
+    path: str | Path,
+    *,
+    start: str | pd.Timestamp | None = None,
+    end: str | pd.Timestamp | None = None,
+) -> pd.DataFrame:
+    """Read a project Calliope timeseries file into a simple dataframe.
+
+    Parameters
+    ----------
+    path
+        Calliope-formatted CSV file.
+    start, end
+        Optional model horizon bounds. If supplied, data are returned over the
+        half-open interval ``[start, end)``.
+
+    Returns
+    -------
+    pandas.DataFrame
+        Hourly numeric features indexed by ``timesteps``.
+    """
+    rows = _read_csv_rows(path)
+    tech_row = _find_marker_row(rows, "techs")
+    timestep_row = _find_marker_row(rows, "timesteps", start=tech_row + 1)
+
+    columns = tuple(value.strip() for value in rows[tech_row][1:] if value.strip())
+    _validate_feature_columns(columns)
+
+    n_fields = len(columns) + 1
+    data_rows = [row[:n_fields] for row in rows[timestep_row + 1 :] if row and row[0].strip()]
+
+    if not data_rows:
+        raise ValueError(f"No timeseries data found in {Path(path)}.")
+
+    frame = pd.DataFrame(data_rows, columns=["timesteps", *columns])
+    frame["timesteps"] = pd.to_datetime(frame["timesteps"], errors="raise")
+
+    for column in columns:
+        frame[column] = pd.to_numeric(frame[column], errors="raise")
+
+    frame = frame.set_index("timesteps")
+    frame.index = pd.DatetimeIndex(frame.index, name="timesteps")
+
+    _validate_regular_timeseries(frame)
+
+    if start is not None or end is not None:
+        frame = slice_timeseries(frame, start=start, end=end)
+
+    return frame
+
+
+def slice_timeseries(
+    data: pd.DataFrame,
+    *,
+    start: str | pd.Timestamp | None = None,
+    end: str | pd.Timestamp | None = None,
+) -> pd.DataFrame:
+    """Return a complete half-open model horizon ``[start, end)``.
+
+    Bounds must align to timestamps in the source data. The end bound may also
+    be exactly one timestep after the final source timestamp.
+    """
+    _validate_regular_timeseries(data)
+
+    start_ts = pd.Timestamp(start) if start is not None else data.index[0]
+    step = _timestep(data.index)
+    end_ts = pd.Timestamp(end) if end is not None else data.index[-1] + step
+
+    if start_ts >= end_ts:
+        raise ValueError("Timeseries start must be earlier than end.")
+
+    if start_ts not in data.index:
+        raise ValueError(f"Requested start {start_ts} is not present in the source data.")
+
+    valid_end = end_ts in data.index or end_ts == data.index[-1] + step
+    if not valid_end:
+        raise ValueError(
+            f"Requested end {end_ts} is neither present in the source data nor "
+            "one timestep after its final timestamp."
+        )
+
+    sliced = data.loc[(data.index >= start_ts) & (data.index < end_ts)].copy()
+    expected_index = pd.date_range(start=start_ts, end=end_ts, freq=step, inclusive="left")
+
+    if not sliced.index.equals(expected_index):
+        missing = expected_index.difference(sliced.index)
+        raise ValueError(
+            "Requested model horizon is incomplete. "
+            f"Missing {len(missing)} timestep(s); first missing values: "
+            f"{missing[:5].tolist()}"
+        )
+
+    return sliced
+
+
+def write_calliope_timeseries(
+    data: pd.DataFrame,
+    path: str | Path,
+    *,
+    template_path: str | Path,
+) -> Path:
+    """Write a simple dataframe back to the project Calliope CSV format.
+
+    Metadata rows (comments, nodes, technologies, and parameters) are copied
+    from ``template_path``. Only the hourly data section is replaced. This
+    keeps the idiosyncratic Calliope table structure at the edge of the
+    workflow while allowing TSAM and the SoC Proxy to work with ordinary
+    dataframes internally.
+    """
+    _validate_regular_timeseries(data)
+
+    rows = _read_csv_rows(template_path)
+    tech_row = _find_marker_row(rows, "techs")
+    timestep_row = _find_marker_row(rows, "timesteps", start=tech_row + 1)
+    template_columns = tuple(
+        value.strip() for value in rows[tech_row][1:] if value.strip()
+    )
+
+    _validate_feature_columns(template_columns)
+
+    missing = set(template_columns) - set(data.columns)
+    extra = set(data.columns) - set(template_columns)
+    if missing or extra:
+        raise ValueError(
+            "Timeseries columns do not match the Calliope template. "
+            f"Missing: {sorted(missing)}; extra: {sorted(extra)}"
+        )
+
+    output = Path(path)
+    output.parent.mkdir(parents=True, exist_ok=True)
+
+    with output.open("w", encoding="utf-8", newline="") as file:
+        writer = csv.writer(file, lineterminator="\n")
+        writer.writerows(rows[: timestep_row + 1])
+
+        ordered = data.loc[:, template_columns]
+        for timestamp, values in ordered.iterrows():
+            writer.writerow(
+                [timestamp.strftime("%Y/%m/%d %H:%M")]
+                + [_format_numeric(value) for value in values]
+            )
+
+    return output
+
+
+def _read_csv_rows(path: str | Path) -> list[list[str]]:
+    source = Path(path)
+    if not source.is_file():
+        raise FileNotFoundError(f"Timeseries file does not exist: {source}")
+
+    with source.open("r", encoding="utf-8-sig", newline="") as file:
+        return list(csv.reader(file))
+
+
+def _find_marker_row(
+    rows: list[list[str]],
+    marker: str,
+    *,
+    start: int = 0,
+) -> int:
+    marker = marker.lower()
+    for index, row in enumerate(rows[start:], start=start):
+        if row and row[0].strip().lower() == marker:
+            return index
+    raise ValueError(f"Could not find {marker!r} row in Calliope timeseries file.")
+
+
+def _validate_feature_columns(columns: Iterable[str]) -> None:
+    columns = tuple(columns)
+    if columns != EXPECTED_FEATURE_COLUMNS:
+        raise ValueError(
+            "Unexpected Calliope timeseries feature columns. "
+            f"Expected {EXPECTED_FEATURE_COLUMNS}, received {columns}."
+        )
+
+
+def _validate_regular_timeseries(data: pd.DataFrame) -> None:
+    if not isinstance(data, pd.DataFrame):
+        raise TypeError("Timeseries data must be a pandas DataFrame.")
+    if data.empty:
+        raise ValueError("Timeseries data cannot be empty.")
+    if not isinstance(data.index, pd.DatetimeIndex):
+        raise TypeError("Timeseries data must use a DatetimeIndex.")
+    if data.index.has_duplicates:
+        raise ValueError("Timeseries timestamps must be unique.")
+    if not data.index.is_monotonic_increasing:
+        raise ValueError("Timeseries timestamps must be monotonically increasing.")
+    if data.isna().any().any():
+        raise ValueError("Timeseries data contain missing values.")
+
+    _timestep(data.index)
+
+
+def _timestep(index: pd.DatetimeIndex) -> pd.Timedelta:
+    if len(index) < 2:
+        raise ValueError("At least two timestamps are required.")
+
+    differences = index.to_series().diff().dropna().unique()
+    if len(differences) != 1:
+        raise ValueError("Timeseries data must have a regular timestep.")
+
+    step = pd.Timedelta(differences[0])
+    if step <= pd.Timedelta(0):
+        raise ValueError("Timeseries timestep must be positive.")
+    return step
+
+
+def _format_numeric(value: object) -> str:
+    if pd.isna(value):
+        raise ValueError("Cannot write missing values to Calliope timeseries.")
+    return format(float(value), ".15g")
diff --git a/scripts/pipeline.py b/scripts/pipeline.py
index f7a8e22..85f157d 100644
--- a/scripts/pipeline.py
+++ b/scripts/pipeline.py
@@ -3,6 +3,7 @@
 from __future__ import annotations
 
 from dataclasses import dataclass
+from pathlib import Path
 from typing import Any
 
 import pandas as pd
@@ -10,6 +11,7 @@ from tsam import AggregationResult
 
 from soc_proxy import generate_soc_proxy
 
+from scripts.helpers.timeseries import read_calliope_timeseries
 from scripts.helpers.tsa import (
     build_cluster_config,
     build_weights,
@@ -32,6 +34,26 @@ class TSAArtifacts:
     reconstructed_proxy: pd.DataFrame
 
 
+def load_case_timeseries(
+    config: dict[str, Any],
+    source: str | Path,
+) -> pd.DataFrame:
+    """Load the exact model horizon defined by one resolved experiment.
+
+    The project source files use Calliope's multi-row CSV format. This adapter
+    converts that format to the simple dataframe used by the SoC Proxy and
+    TSAM, while enforcing the experiment's half-open ``[start_date, end_date)``
+    horizon.
+    """
+    data_params = config["data_params"]
+
+    return read_calliope_timeseries(
+        source,
+        start=data_params["start_date"],
+        end=data_params["end_date"],
+    )
+
+
 def run_tsa_case(
     config: dict[str, Any],
     timeseries: pd.DataFrame,
@@ -150,7 +172,10 @@ def _build_clustering_features(
     lambda_soc = soc_config["lambda_soc"]
 
     if proxy_column not in proxy.columns:
-        raise ValueError(...)
+        raise ValueError(
+            f"Configured proxy field {proxy_column!r} was not generated. "
+            f"Available fields are: {list(proxy.columns)}"
+        )
 
     if lambda_soc == 0:
         return timeseries.copy(), None
@@ -181,13 +206,9 @@ def _build_tsa_weights(
         return None
 
     if lambda_soc == 1:
-        # Handled upstream by reducing the feature set to the proxy alone.
-        #
-        # We will implement that explicitly once lambda=1 experiments are
-        # included in the experiment portfolio.
-        raise NotImplementedError(
-            "lambda_soc=1 requires clustering on the proxy feature alone."
-        )
+        # Feature construction has already reduced the clustering input to the
+        # proxy alone, so TSAM's default equal weighting is sufficient.
+        return None
 
     return build_weights(
         features.columns,
@@ -205,6 +226,14 @@ def _generate_proxy(
     This adapter is intentionally isolated because the public
     ``generate_soc_proxy`` API is the next component we intend to clean up.
     """
+    if "economic_modifier_m" in params:
+        raise NotImplementedError(
+            "The rebuilt experiment config defines 'economic_modifier_m', "
+            "but generate_soc_proxy() does not yet implement that parameter. "
+            "Implement its intended semantics before running experiments so "
+            "the configured value cannot be silently ignored."
+        )
+
     result, _, _ = generate_soc_proxy(
         df=timeseries,
         renewables_fields_and_weights=params[
